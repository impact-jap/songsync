# Web Notes

The studio is intentionally small and static.

JavaScript owns:

- file input;
- audio playback;
- DOM rendering;
- canvas rendering;
- downloads;
- copy buttons.

WASM owns:

- parsing;
- conversion;
- render nodes;
- cursor state;
- click-to-seek timestamps.
